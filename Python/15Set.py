# Set is a collection of the onodered items.
# Each elements in the set must be unique and immutable.
#Set is mutable but its elments are immutable in nature.


collection = {1,2,3,2,2,"hello","world",4}

print(collection)
print(type(collection))
print(len(collection))


# if we wanted to create an empty set then :

empty = set()
print(type(empty))

